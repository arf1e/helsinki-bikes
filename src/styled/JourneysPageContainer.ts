import styled from 'styled-components';

const JourneysPageContainer = styled.main`
  display: flex;
  flex: 1;
  flex-direction: row;
  justify-content: flex-start;
`;

export default JourneysPageContainer;
