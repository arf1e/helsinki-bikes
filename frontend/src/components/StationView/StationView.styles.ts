import styled from 'styled-components';

const StationViewContainer = styled.div`
  display: flex;
  flex: 1;
  flex-direction: row;
  justify-content: space-between;
`;

export default StationViewContainer;
