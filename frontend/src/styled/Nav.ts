import styled from 'styled-components';

const Nav = styled.nav`
  display: flex;
  margin-top: 48px;
  margin-bottom: 32px;

  ul {
    display: flex;
    flex-direction: row;

    list-style: none;
  }
`;

export default Nav;
